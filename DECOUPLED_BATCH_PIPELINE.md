# 高并发批量处理管线重构完成

我已经成功按照您的要求，将“ASR转录”与“LLM规整”进行了彻底解耦，并对 LLM（尤其是 Gemini）的总结发散行为增加了极强的防护机制。

## 关键改动与成果

### 1. 📂 独立的 OSRT 中间产物目录
我们弃用了原来与用户文档混杂的暂存目录。现在，Phase 1 (ASR) 会将所有中间件统一输出到绝对独立的隔离目录：
```text
/Users/vickers/Movies/OSRT/<视频名>/
├── raw.json   (保留供后续 LLM 读取的片段数据)
└── raw.srt    (原生 ASR 字幕，即刻可用)
```
> [!TIP]
> 现在您只需运行 `batch_transcribe_02_03.py` 的第一阶段，立刻就能获得所有视频的原生可用 `.srt` 字幕。不再被漫长的 LLM 规整过程阻塞。

### 2. 🛡️ 并行共存的规整输出
在执行完 LLM 阶段后（无论是用原脚本的 Phase 2 还是运行常驻守护进程 `parallel_llm_polish.py`），生成的经过优化的最终成品将保存为：
`视频同目录/<视频名>.polished.srt`
- 这意味着原版字幕和优质字幕完美并行。
- 旧有的损坏的 `.srt` 不会再导致程序的“跳过”误判。

### 3. 🚦 双重防总结与熔断机制
为了应对 Gemini 最近的 "总结癖"，我们在请求 Prompt 与 解析逻辑上做了双管齐下的防御：
- **Prompt 铁律**：注入了 `CRITICAL RULES`，明令禁止合并、删除和总结，要求行行对应。
- **行数守恒校验 (80% Fallback)**：解析到结果后，代码会立即校验返回的规整行数。如果丢失量超过 `20%`（即不足 `80%`），程序立刻判定为**疑似总结行为**，主动发起重试（上限 4 次）。若重试全部用尽，则主动放弃这一小段，保留原文，从而达到**“宁可不改，也绝不丢字”**的稳定效果。

### 4. 🗂️ 职责分明的状态机
在底层的 `manifest.json` 中，状态标记不再是模糊的一个 `srt_done`：
- `asr_done`: 仅代表 ASR 完成，有原生 SRT 了。
- `polished_done`: 仅代表 LLM 校对完成，有优质字幕了。
后续如果需要对所有的 `polished.srt` 重新跑一遍其他模型，直接把 `polished_done` 改回 `false` 即可，不用再重做昂贵的 ASR。

---

现在，整套批量 ASR 与规整服务已变得极其坚固（Robust），您现在可以继续放心地投入极大规模的视频批量转录任务了。
